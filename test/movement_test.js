const assert = require('assert');
const ustils = require('./utils');
const game = require('../src/game');

describe('Moving', () => {
    it('should be allowed only after game started', () => {
        
    })
})